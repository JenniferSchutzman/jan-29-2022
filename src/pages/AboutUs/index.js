import React from "react";

const AboutUs = ({ data }) => {
  // const data = props.data
  // const { data } = props;
  console.log("data", data);
  return <div>this is abotu us</div>;
};

export default AboutUs;
